(ert-deftest add-rule ()
  ;; TODO: Add tests for add-rule
  )

(ert-deftest clear-internal-rules ()
  ;; TODO: Add tests for clear-internal-rules
  )

(ert-deftest path-ignored-p ()
  ;; TODO: Add tests for path-ignored-p
  )
